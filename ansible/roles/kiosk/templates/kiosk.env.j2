# Von Ansible erzeugt - Aenderungen von Hand werden beim naechsten Pull
# ueberschrieben. Genau das ist der Zweck: eine hier verstellte URL ist Drift
# und heilt zentral, nicht durch Handauflegen.
#
# Geraet {{ deviceId }} · Raum {{ roomId | default('nicht zugewiesen') }} · Version {{ configVersion }}
KIOSK_URL={{ displayUrl | default('http://127.0.0.1:' ~ display_port ~ '/display.html', true) }}
KIOSK_BROWSER={{ kiosk.browser | default('cog') }}
